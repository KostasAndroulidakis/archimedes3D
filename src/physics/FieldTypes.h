#pragma once

namespace Archimedes {

// Field types enum definition (moved to separate file to avoid redefinition)
enum class FieldType {
    Electric,
    Magnetic,
    Plasma
};

} // namespace Archimedes